import PogObject from "../../../PogData";
import config from "../../config";


const render = register('renderWorld', () => {

})

export function toggle() {
    if (config().configToggle && config().toggle && config().toggleCheat) {

        return
    }

    return
}
export default { toggle };